using System;
using Foundation;
using ObjCRuntime;

namespace DatadogMaui.iOS.Flags
{
    // DatadogFlags is a pure Swift module with no @objc public API surface.
    // The DatadogFlags-Swift.h header contains no ObjC-exposed types across
    // any architecture slice, so there are no interfaces to bind here.
    //
    // This project exists solely to carry the DatadogFlags.xcframework as a
    // NativeReference so it is linked into the final app bundle.
}
